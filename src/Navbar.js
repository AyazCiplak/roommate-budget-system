const Navbar = () => {
    return ( 
        <nav className="navbar">
            <h1>(Title)</h1>
            <div className="links">
                <a href="/">Home</a>
                {
                //Add new link when page is set up
                }
                <a href="/">New Expense</a>
            </div>
        </nav>
     );
}
 
export default Navbar;