const Navbar = () => {
    return ( 
        <nav className="navbar">
            <h1>(Title)</h1>
            <div className="links">
                {
                //Add new link when page is set up
                }
                <a href="/">New User</a>
                <a href="/">New Expense</a>
                <a href='/'>Settings</a>
                <a href="/">Home</a>
            </div>
        </nav>
     );
}
 
export default Navbar;